export default function Home() {
  // this should just be the landing page which is neatly designed and prompts the user to either directly find offers (search bar or subject selection) and if pressed it will get the user to the /nachhilfe-suchen page which their requested filters
  // we also want a button to start offering tutoring which will prompt the user to login if not already logged in
  return <>Hier steht echt viel kacke...</>;
}
